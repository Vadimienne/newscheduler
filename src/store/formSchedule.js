// get todays day and form an initial schedule

export const formSchedule = (todos, tempTodos) => {
    // let schedule = []

    // // find todos only for today
    // let today = new Date()
    // let dayNum = today.getDay()
    // let todayTodos = todos.filter(el => 
    //     Array.isArray(el.days) && el.days.includes(dayNum)
    // )

    // schedule = tempTodos.concat(todayTodos)

    // return schedule
}